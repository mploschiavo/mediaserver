# Compose Auth Provider Defaults

This folder contains provider-local default config assets for compose auth providers.

- `authelia/`: seeded into `${CONFIG_ROOT}/authelia` on first start.

For production/internet exposure, replace all default secrets and update provider config before enabling auth.
