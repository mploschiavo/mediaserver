# Diagrams

Primary architecture diagrams are stored in `docs/diagrams/`.
Software design model diagrams are also stored there (component model, adapter model, runtime model).

Render/update diagrams:
```bash
bash scripts/render-architecture-diagrams.sh
```
