"""User-management infrastructure — UserStore, AuditLog, BanStore,
InviteStore, PasswordTicketStore, RoleCatalog, NullProvider, and the
SafeJson/Yaml editors that back them.

Migrated from ``media_stack.core.auth.users`` in ADR-0002 Phase 16-B.
"""
