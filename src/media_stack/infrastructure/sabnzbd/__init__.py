"""SABnzbd-specific infrastructure.

ADR-0002 Phase 16-D batch 3 (download clients) — tech-specific I/O:
HTTP preflight, compose preflight (docker exec), CLI api-access
reconciler (kubectl exec). No business policy lives here.
"""
