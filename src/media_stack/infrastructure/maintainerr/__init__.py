"""Maintainerr infrastructure layer.

ADR-0002 Phase 16-D batch 4 (maintainerr) — reserved for tech-specific
I/O modules (HTTP, file ops). Currently empty; Maintainerr's HTTP/file
I/O is injected as callables from the runtime-ops boundary.
"""
