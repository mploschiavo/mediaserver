"""Tautulli service placeholder for future adapter-backed orchestration."""

from dataclasses import dataclass


@dataclass
class TautulliService:
    """No-op placeholder until Tautulli bootstrap orchestration is extracted."""

    def configure(self, *args, **kwargs) -> None:
        return
