"""Homepage app-layer constants.

Exports the service slug and label so that platform code can reference
the dashboard service without hardcoding its name.
"""

#: URL slug used in path-prefix routing (e.g. /app/homepage).
DASHBOARD_SLUG = "homepage"

#: Human-readable label for log messages.
DASHBOARD_LABEL = "homepage"
