"""Maintainerr domain layer.

ADR-0002 Phase 16-D batch 4 (maintainerr) — reserved for pure
transforms (rule translation primitives that are simple data
manipulation). Currently empty; the rule-translation service has
injected I/O ports so it lives in ``application/maintainerr``.
"""
