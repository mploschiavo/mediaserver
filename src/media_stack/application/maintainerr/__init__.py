"""Maintainerr application-layer use cases.

ADR-0002 Phase 16-D batch 4 (maintainerr) — orchestrates Maintainerr
policy artifact generation, rule translation, rule sync, and
integration reconcile use cases. Depends on ports injected at the
runtime-ops boundary; tech-specific I/O lives in
``infrastructure/maintainerr``.
"""
