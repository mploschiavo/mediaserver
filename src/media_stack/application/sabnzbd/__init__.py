"""SABnzbd application-layer use cases.

ADR-0002 Phase 16-D batch 3 (download clients) — orchestrates SABnzbd
configure/ensure/reconcile use cases (api-key resolution, defaults,
categories, download-client wiring). Depends on domain/sabnzbd/* and
ports declared in interfaces/.
"""
