"""Auth adapters — provider-specific glue (Authelia, Authentik, ...)
plus the discovery registry that walks ``providers/`` for plugin specs.

Migrated from ``media_stack.core.auth`` in ADR-0002 Phase 16-B.
"""
