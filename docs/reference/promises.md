<!-- AUTO-GENERATED by bin/render-promises-reference.py -->
<!-- Source: contracts/promises.yaml. Edit the YAML, then re-run the script. -->

# Promises Reference

Every claim like "after a fresh install, Bazarr has English subtitles enabled" lives in [`contracts/promises.yaml`](../../contracts/promises.yaml) as a single declarative entry. Three things consume that file:

1. The meta-ratchet (`tests/unit/test_promises_registry.py`) — verifies every promise points at a real handler.
2. The fresh-install verifier (`bin/verify-fresh-install.sh`) — runs each promise's probe against a live stack.
3. This page — the human-readable index.

## How to read the table

- **Promise** — the claim. After a clean bootstrap, the promise should hold and stay holding.
- **Ensured by** — the contract job (or `post_setup_handler`) whose handler makes the promise true.
- **Probe** — what the verifier checks to prove the promise actually holds in a live stack.

There are **33 promises** at this commit.

## The promises

| Promise | Ensured by | Probe |
|---|---|---|
| **bazarr-language-profile** &mdash; Bazarr has at least one language profile (default English) | `ensure-bazarr-language-profile` | `http_json` &rarr; bazarr/api/system/languages/profiles |
| **bazarr-default-profile-toggles** &mdash; Bazarr auto-assigns the default profile to new series + movies | `ensure-bazarr-language-profile` | `http_json` &rarr; bazarr/api/system/settings |
| **bazarr-providers** &mdash; Bazarr has the curated subtitle provider set enabled | `ensure-bazarr-language-profile` | `http_json` &rarr; bazarr/api/system/settings |
| **bazarr-arr-integration** &mdash; Bazarr has Sonarr + Radarr connections configured | `ensure-bazarr-language-profile` | `http_json` &rarr; bazarr/api/system/settings |
| **bazarr-jellyfin-plugin-config** &mdash; Jellyfin Bazarr plugin config has the right URL + API key | `ensure-bazarr-language-profile` | `file_text` &rarr; jellyfin/plugins/configurations/Jellyfin.Plugin.Bazarr.xml |
| **jellyseerr-oidc** &mdash; Jellyseerr login page shows the "Sign in with Authelia" button | `ensure-jellyseerr-oidc` | `http_json` &rarr; jellyseerr/api/v1/settings/public |
| **jellyseerr-application-url** &mdash; Jellyseerr applicationUrl is set HTTPS so OIDC redirect_uri matches Authelia's registered list | `ensure-jellyseerr-oidc` | `file_json` &rarr; jellyseerr/settings.json |
| **sonarr-jellyfin-notifier** &mdash; Sonarr has the media-stack-jellyfin MediaBrowser notifier | `ensure-arr-jellyfin-notifier` | `http_json` &rarr; sonarr/app/sonarr/api/v3/notification |
| **radarr-jellyfin-notifier** &mdash; Radarr has the media-stack-jellyfin MediaBrowser notifier | `ensure-arr-jellyfin-notifier` | `http_json` &rarr; radarr/app/radarr/api/v3/notification |
| **lidarr-jellyfin-notifier** &mdash; Lidarr has the media-stack-jellyfin MediaBrowser notifier | `ensure-arr-jellyfin-notifier` | `http_json` &rarr; lidarr/app/lidarr/api/v1/notification |
| **prowlarr-indexers-discovered** &mdash; Prowlarr has at least one indexer from the curated allowlist | `discover-indexers` | `http_json` &rarr; prowlarr/app/prowlarr/api/v1/indexer |
| **prowlarr-indexers-tagged** &mdash; Tag-indexers ran and produced sync-* tags on at least one indexer | `tag-indexers-for-apps` | `http_json` &rarr; prowlarr/app/prowlarr/api/v1/indexer |
| **sonarr-has-indexers** &mdash; Sonarr received at least one indexer from Prowlarr push-sync | `push-indexers` | `http_json` &rarr; sonarr/app/sonarr/api/v3/indexer |
| **radarr-has-indexers** &mdash; Radarr received at least one indexer from Prowlarr push-sync | `push-indexers` | `http_json` &rarr; radarr/app/radarr/api/v3/indexer |
| **sonarr-has-series** &mdash; Sonarr has the seed-series titles + the popular-TV CustomImport | `ensure-sonarr-seed-series` | `http_json` &rarr; sonarr/app/sonarr/api/v3/series |
| **maintainerr-rules-linked-to-arr** &mdash; Movie rules link to Radarr; TV rules link to Sonarr (not None) | `configure-collections` | `http_json` &rarr; maintainerr/app/maintainerr/api/collections |
| **radarr-import-lists-auto** &mdash; Radarr import lists have enableAuto=true (TMDb popular movies auto-add) | `apply-arr-runtime-defaults` | `http_json` &rarr; radarr/app/radarr/api/v3/importlist |
| **qbittorrent-categories** &mdash; qBit has the four content categories (movies/tv/music/books) | `ensure-qbittorrent-categories` | `http_text` &rarr; qbittorrent/api/v2/torrents/categories |
| **sonarr-download-client** &mdash; Sonarr has qBit configured as enabled download client (cat=tv) | `ensure-arr-download-client` | `http_json` &rarr; sonarr/app/sonarr/api/v3/downloadclient |
| **radarr-download-client** &mdash; Radarr has qBit configured as enabled download client (cat=movies) | `ensure-arr-download-client` | `http_json` &rarr; radarr/app/radarr/api/v3/downloadclient |
| **jellyfin-libraries** &mdash; Jellyfin has Movies, TV Shows, Music, Books libraries pointing at /media/* | `ensure-jellyfin-libraries` | `http_json` &rarr; jellyfin/Library/VirtualFolders |
| **jellyseerr-arr-servers** &mdash; Jellyseerr has Sonarr+Radarr+Jellyfin connections with API keys | `configure-jellyseerr` | `file_json` &rarr; jellyseerr/settings.json |
| **sonarr-quality-profiles** &mdash; Sonarr has at least one quality profile available | `apply-arr-runtime-defaults` | `http_json` &rarr; sonarr/app/sonarr/api/v3/qualityprofile |
| **radarr-quality-profiles** &mdash; Radarr has at least one quality profile available | `apply-arr-runtime-defaults` | `http_json` &rarr; radarr/app/radarr/api/v3/qualityprofile |
| **gateway-https-listener-up** &mdash; Envoy's HTTPS listener accepts TLS handshakes on host:443 | `envoy-config` | `http_text` &rarr; gateway_https/.well-known/openid-configuration |
| **gateway-jellyfin-route** &mdash; Jellyfin reachable via subdomain through Envoy gateway | `envoy-config` | `http_text` &rarr; gateway_https/System/Info/Public |
| **gateway-app-prefix-route** &mdash; Path-prefix routing works through Envoy (apps.media-stack.local/app/<svc>/) | `envoy-config` | `http_text` &rarr; gateway_https/app/authelia/ |
| **gateway-http-redirects-to-https** &mdash; Plain http://<gateway-host>/ returns 301 to https:// | `envoy-config` | `http_status` &rarr; gateway_http/ |
| **stuck-imports-scheduled** &mdash; recover-stuck-imports runs every 30min to catch qBit-completed-but-not-imported files | `scan-completed-downloads` | `http_json` &rarr; controller/api/schedules |
| **adaptive-search-scheduled** &mdash; mass-search-throttled is scheduled hourly so empty/idle stacks keep grinding for grabs | `scan-completed-downloads` | `http_json` &rarr; controller/api/schedules |
| **foundational-jobs-run-before-app-jobs** &mdash; configure-auth + envoy-config live in the infrastructure phase (before media_server/download_clients) | `configure-auth` | `http_json` &rarr; controller/api/jobs |
| **unpackerr-config-valid** &mdash; Unpackerr config has resolved arr URLs (not literal service_internal_url() text) | `unpackerr-post` | `file_text` &rarr; unpackerr/unpackerr.conf |
| **dns-readiness-banner-data** &mdash; Routing-probe endpoint returns enough data for the dashboard's DNS banner to render | `envoy-config` | `http_json` &rarr; controller/api/routing |

## Adding a new promise

1. Write the `ensure_*` adapter that re-asserts the desired state. Idempotent &mdash; every run leaves the system in the same end state.
2. Register a contract job pointing at the adapter (`phase: post`).
3. Add an entry to [`contracts/promises.yaml`](../../contracts/promises.yaml) with a probe that proves it works.
4. Re-run this script: `python3 bin/render-promises-reference.py`.

If steps 1+2 happen but step 3 doesn't, the meta-ratchet warns (orphan adapter); if step 3 happens without 1+2, the meta-ratchet fails hard (broken promise).

---

**Project Steward**
Matthew Loschiavo &middot; [matthewloschiavo.com](https://matthewloschiavo.com) &middot; [mploschiavo@gmail.com](mailto:mploschiavo@gmail.com) &middot; [LinkedIn](https://www.linkedin.com/in/matthewloschiavo)
